Classroom Info
<%= render "classroom" %>

<% @classroom.students.each do |student| %>
#render partial: "students/student", locals: {student: @classroom.student}
<%= render partial: "student", locals: {student: student} %>
<% end %>